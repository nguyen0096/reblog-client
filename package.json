{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream --parallel",
    "clean": "lerna clean -y | find . -name 'yarn.lock' -delete | rm -rf node_modules"
  }
}