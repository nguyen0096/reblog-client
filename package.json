{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream --parallel",
    "clean": "lerna clean | find . -name 'yarn.lock' -delete"
  }
}