# ESLint

- For typescript, typescriptreact, to make eslint validate on those files, in vscode setting, must define:

``` JSON
"eslint.validate": [
        "javascript",
        "javascriptreact",
        "html",
        "typescriptreact"
    ],
```
